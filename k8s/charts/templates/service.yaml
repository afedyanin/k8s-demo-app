apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuber-demo-app.fullname" . }}
  labels:
    {{- include "kuber-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kuber-demo-app.selectorLabels" . | nindent 4 }}
